// Main file that imports from math module

import { add, multiply, PI } from "./math";

const x = 10;
const y = 5;

const sum = add(x, y);
const product = multiply(x, y);

console.log("Sum:");
console.log(sum);

console.log("Product:");
console.log(product);

console.log("PI:");
console.log(PI);
